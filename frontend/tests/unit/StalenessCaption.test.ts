import { describe, it } from 'vitest';

describe('StalenessCaption visibility (UI-14)', () => {
  it.skip('hides entirely when freshness=fresh', () => {
    /* Plan 08-04 fills */
  });
  it.skip('shows when amber', () => {
    /* Plan 08-04 fills */
  });
  it.skip('shows when red', () => {
    /* Plan 08-04 fills */
  });
});
