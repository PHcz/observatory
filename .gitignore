# GSD internal planning scaffolding — kept local, not published.
# Contains the OS username in @-reference paths and is build-process noise,
# not public documentation. Untracked before the public flip (Phase 9, 09-05
# Finding 4a). Re-enable locally by removing this line if needed.
.planning/

.DS_Store
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
node_modules/
.svelte-kit/
build/
dist/
*.db
*.db-journal
*.sqlite
*.sqlite-journal
.env
.env.local
.idea/
.vscode/

# Phase 1 additions
.ruff_cache/
.mypy_cache/
.pytest_cache/
frontend/build/
frontend/.svelte-kit/
*.local
/tmp/

# Phase 3 — dev Mosquitto + secrets
dev/mosquitto/data/

# Real Mosquitto password files (never commit) — only .example is tracked
deploy/mosquitto/passwords
/etc/mosquitto/passwords
passwords

# Phase 8 / OPS-06 additions — completeness pass before public release
# SQLite WAL + SHM sidecars (live when journal_mode=WAL is on)
*.db-wal
*.db-shm
*.sqlite3
*.sqlite3-journal
# All .env variants except the tracked .example template
.env.*
!.env.example
# Common secret-file names
secrets.env
secret.env
*.pem
*.key
# Local-only sensitive-pattern list for the pre-commit sensitive-data gate
.git-sensitive-patterns
# Operator-only internal build status (moved out of the public CLAUDE.md)
STATUS.local.md
# Operator-local soak evidence (telemetry snapshots — may carry IP / hostname)
/var/lib/observatory/
# Editor noise
*.swp
*.swo

# Local Claude Code tooling config (preview launcher, local permissions) — not for the public repo
.claude/

# ESPHome indoor-air node — real secrets + build artifacts stay local
firmware/indoor-air/secrets.yaml
firmware/indoor-air/.esphome/
firmware/**/.esphome/
